・個人設定を実装する
    ・すべてのアプリを区別するか、アプリごとにまとめるかを選べるようにする
        (例：a.txtをメモ帳で開くのとb.txtをメモ帳で開くのを区別するかどうか。)
    ・上記にあわせて、正規表現でまとめるアプリをカスタムできるようにする
        (例：「*a.txt*」を登録したらa.txtが含まれるものを一つにまとめる)
    ・同様に、正規表現に一致するアプリだけを表示(もしくは非表示)にできるようにする
    ・マウスやキーボードの操作回数で足きりしてグラフを表示できるようにする

・UIを使いやすくする
    ・表示する範囲をキー入力で指定(現状指定できるのは左端の時刻のみ)
    ・棒グラフを1時間毎、3時間毎...と、指定の時間幅で一つの棒グラフとして表示できるようにする
    ・キー入力で用意された時間幅以外でも一つの棒グラフにできるようにする
    ・グラフを左右に移動するボタンは、一回の入力で棒グラフ一つ分移動する(=棒グラフの設定で移動幅を変える)
    ・グラフの横幅を変えるボタンは、一回の入力で棒グラフ一つ分を左右それぞれで増減させる
    ・棒グラフにマウスオーバーしたらアプリ名、時間、マウスとキーボードの操作回数を表示させる
    ↓実装できるか不明
    ・グラフをドラッグして左右に動かしたらグラフが移動いどうするようにする
    ・同じアプリ名は、違う棒グラフでも同じ色で表示する
    ・タブのサイズを大きくする(オプションになさそう)